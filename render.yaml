
services:
  - type: worker
    name: pocket-option-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && pip install websockets"
    startCommand: "python main.py"
